<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.miki.znacenjePrezimena">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.DetailActivity"
            android:label="@string/detail_section"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.ContactActivity"
            android:label="@string/contact_section"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.AboutActivity"
            android:label="@string/about_section"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.VideoActivity"
            android:label="@string/video_section"
            android:parentActivityName=".ui.MainActivity" />
    </application>

</manifest>